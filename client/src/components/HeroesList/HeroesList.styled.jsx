import styled from '@emotion/styled';

export const List = styled.ul`
    display: flex;
    flex-wrap: wrap;
    max-width: 800px;
    margin-top: -20px;
    margin-right: -20px;
    margin-bottom: 10px;
`;